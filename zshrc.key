export LOGNAME=key
export USER=key

alias vf="git grep --open-files-in-pager=$EDITOR"
alias dcrun="docker-compose run"

function dspec() {
  [ -f 'docker-compose.yml' ] || app=$(basename $(pwd))
  dcrun -w /app/$app console bundle exec rspec $1
}
